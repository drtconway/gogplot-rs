input:
  file: ../mtcars.csv
plot:
  mapping:
    x: ~mpg
  layers:
    - geom: histogram
      stat: bin
      params:
        bins: 10
        fill: darkgreen
        color: black
        alpha: 0.7
output:
  filename: histogram_basic.png
  width: 800
  height: 600

input:
  file: ../mtcars.csv
plot:
  mapping:
    x: wt
    y: mpg
  layers:
    - geom: point
      mapping:
        color: $cyl
        size: ~hp
      params:
        alpha: 0.8
output:
  filename: basic_point.png
  format: png
  width: 800
  height: 600
  dpi: 96

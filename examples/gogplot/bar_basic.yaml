input:
  file: ../mtcars.csv
plot:
  mapping:
    x: $cyl
  layers:
    - geom: bar
      stat: count
      params:
        width: 0.7
        fill: steelblue
        color: black
        alpha: 0.8
output:
  filename: bar_basic.png
  width: 800
  height: 600
